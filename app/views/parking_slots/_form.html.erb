<%= form_with(model: parking_slot) do |form| %>
  <% if parking_slot.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parking_slot.errors.count, "error") %> prohibited this parking_slot from being saved:</h2>

      <ul>
        <% parking_slot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :slot_number, style: "display: block" %>
    <%= form.number_field :slot_number %>
  </div>

  <div>
    <%= form.label :slot_loaction, style: "display: block" %>
    <%= form.text_field :slot_loaction %>
  </div>

  <div>
    <%= form.label :availability_status, style: "display: block" %>
    <%= form.check_box :availability_status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
